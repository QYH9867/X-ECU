```markdown
Folder `module` includes `Arm.py` and `Tricore.py` are the corresponding modules. 
`Arm.txt` and `Tricore.txt` are firmware converted to a common format (hexadecimal). 
You only need to run `X-ECU.py` and follow the prompts for a quick analysis of the firmware.
```
